[pytest]
addopts = -ra
junit_family = xunit2
# Default markers can be declared here later (e.g., azure)
pythonpath = .
markers =
	azure: tests that hit real Azure resources (requires AZURE_STORAGE_CONNECTION_STRING)
	slow: comparatively slow tests (e.g., parsing large OSIS file)
